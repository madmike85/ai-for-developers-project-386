# Production Environment Variables
# Copy this file to .env and update with your secure credentials

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_this_to_secure_password

# These variables are automatically constructed from above:
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/call_calendar?schema=public
